<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!--
        Android 11 (API 30)dan boshlab, boshqa ilovaning o'rnatilganligini
        tekshirish ("getLaunchIntentForPackage") uchun uni shu yerda aniq
        e'lon qilish SHART, aks holda paket o'rnatilgan bo'lsa ham `null`
        qaytadi (avvalgi auditda topilgan eng jiddiy xato — shu yerda
        tuzatildi).
    -->
    <queries>
        <package android:name="org.telegram.messenger" />
    </queries>

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <service
            android:name=".DaseAccessibilityService"
            android:label="@string/accessibility_service_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">

            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />

        </service>

        <!--
            10-bosqich (fon rejimi) POYDEVORI. Tur sifatida "specialUse"
            tanlandi, chunki bu servis hozircha faqat doimiy bildirishnoma
            ko'rsatadi va tegishli oldindan belgilangan turlardan (masalan
            "microphone" yoki "mediaPlayback") biriga TO'G'RI KELMAYDI —
            hozircha mikrofonni o'zi ishlatmaydi. "microphone" turini
            haqiqatda mikrofon shu servis ichida ishlatilmaguncha yozish
            noto'g'ri (aniq bo'lmagan) bo'lardi.
        -->
        <service
            android:name=".DaseForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="voice_assistant_standby_notification" />
        </service>

    </application>

</manifest>
